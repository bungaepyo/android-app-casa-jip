<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/chat_drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".chat.ChatMainActivity">


    <include layout="@layout/content_chat"/>

    <com.google.android.material.navigation.NavigationView
        app:headerLayout="@layout/profile_header"
        android:id="@+id/chat_profile_drawer"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="@color/white"
        app:itemTextColor="@color/darkGray"
        app:itemIconTint="@color/darkGray"
        app:menu="@menu/menu_left_profile"
        android:layout_gravity="left">

    </com.google.android.material.navigation.NavigationView>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/chat_setting_drawer"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="@color/white"
        app:itemTextColor="@color/darkGray"
        app:itemIconTint="@color/darkGray"
        app:menu="@menu/menu_drawer"
        android:layout_gravity="right">

    </com.google.android.material.navigation.NavigationView>

</androidx.drawerlayout.widget.DrawerLayout>